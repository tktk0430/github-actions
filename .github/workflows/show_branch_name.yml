name: show_branch_name

on:
  push:
    branches:
      - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${GITHUB_HEAD_REF}
      - run: echo ${GITHUB_BASE_REF}
      - run: echo ${GITHUB_REF}
